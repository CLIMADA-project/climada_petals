name: CI Night - Daily Quality Gate

on:
  workflow_dispatch:

permissions:
  actions: read
  contents: read

jobs:
  noop:
    name: Dummy CI Night Job
    runs-on: ubuntu-latest
    steps:
      - name: Dummy step
        run: |
          echo "CI Night placeholder workflow."
