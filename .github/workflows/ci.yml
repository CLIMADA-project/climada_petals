name: GitHub CI

on: [push]

jobs:
  ci:
    name: GitHub CI
    uses: ./.github/workflows/testing.yml
    with:
      petals_branch: ${{ github.ref }}
    permissions:
      # For publishing results
      checks: write
